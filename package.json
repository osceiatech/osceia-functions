{
  "name": "osceia-functions",
  "version": "1.0.0",
  "description": "Cloud Functions para sincronização de usuários com Twenty CRM.",
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint --ext .js,.ts src/",
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "keywords": [
    "firebase",
    "functions",
    "twenty-crm"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5"
  }
}
